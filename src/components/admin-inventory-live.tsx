"use client";

import { Boxes, CircleAlert, PackageCheck, Plus } from "lucide-react";
import { useEffect, useState } from "react";

type InventoryRow = { id: string; name: string; sku: string; stock: number; reserved_stock: number; low_stock_threshold: number; category?: { name?: string } | { name?: string }[] | null };
export function LiveInventoryView() { const [items, setItems] = useState<InventoryRow[]>([]); const [error, setError] = useState(""); useEffect(() => { fetch("/api/admin/inventory").then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error); setItems(body.items); }).catch((caught) => setError(caught instanceof Error ? caught.message : "Unable to load inventory.")); }, []); async function adjust(productId: string, quantity: number) { const response = await fetch("/api/admin/inventory", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ productId, quantity, reason: quantity > 0 ? "Admin stock addition" : "Admin stock removal" }) }); const body = await response.json(); if (!response.ok) { setError(body.error); return; } setItems((current) => current.map((item) => item.id === productId ? { ...item, stock: body.stock } : item)); }
return <><div className="admin-page-heading"><div><p className="admin-eyebrow">Catalog operations</p><h1>Inventory</h1><p>Keep every NUTVORA product ready to ship.</p></div><button className="admin-dark-button"><Plus size={15} /> Adjust stock</button></div>{error && <p className="admin-form-error">{error}</p>}<div className="inventory-summary"><div><Boxes size={18} /><span>Units tracked</span><strong>{items.reduce((sum, item) => sum + item.stock, 0)}</strong></div><div><CircleAlert size={18} /><span>Needs attention</span><strong>{items.filter((item) => item.stock <= item.low_stock_threshold).length} products</strong></div><div><PackageCheck size={18} /><span>Available units</span><strong>{items.reduce((sum, item) => sum + item.stock - item.reserved_stock, 0)}</strong></div></div><section className="admin-panel"><div className="panel-heading"><h2>Stock levels</h2></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Product</th><th>SKU</th><th>Current stock</th><th>Reserved</th><th>Available</th><th>Threshold</th><th>Status</th><th /></tr></thead><tbody>{items.map((item) => { const available = item.stock - item.reserved_stock; const state = item.stock === 0 ? "out" : item.stock <= item.low_stock_threshold ? "low" : "good"; const category = Array.isArray(item.category) ? item.category[0]?.name : item.category?.name; return <tr key={item.id}><td><strong>{item.name}</strong><small>{category ?? "Uncategorized"}</small></td><td>{item.sku}</td><td><strong>{item.stock}</strong></td><td>{item.reserved_stock}</td><td>{available}</td><td>{item.low_stock_threshold}</td><td><span className={`stock-status ${state}`}><i />{state === "good" ? "In stock" : state === "low" ? "Low stock" : "Out of stock"}</span></td><td><div className="stock-controls"><button onClick={() => adjust(item.id, -1)} title="Remove stock">−</button><button onClick={() => adjust(item.id, 1)} title="Add stock">+</button></div></td></tr>; })}</tbody></table>{!items.length && !error && <div className="admin-empty"><strong>No products yet</strong><span>Run the catalog schema and add products to see inventory.</span></div>}</div></section></>; }
