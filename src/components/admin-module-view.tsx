"use client";
import { Check, MoreHorizontal, Plus, Search } from "lucide-react";
import { useState } from "react";
export function CatalogModuleView({ title, eyebrow, copy, items }: { title: string; eyebrow: string; copy: string; items: string[] }) { const [active, setActive] = useState(items.map(() => true)); return <><div className="admin-page-heading"><div><p className="admin-eyebrow">{eyebrow}</p><h1>{title}</h1><p>{copy}</p></div><button className="admin-dark-button"><Plus size={15} /> Add {title.slice(0, -1).toLowerCase()}</button></div><section className="admin-panel"><div className="panel-heading"><h2>All {title.toLowerCase()}</h2><label className="table-search"><Search size={15} /><input placeholder={`Search ${title.toLowerCase()}...`} /></label></div><div className="module-grid">{items.map((item, index) => <div className="module-card" key={item}><div className="module-art">{item.slice(0, 1)}</div><div><strong>{item}</strong><small>{active[index] ? "Available for sale" : "Hidden from store"}</small></div><button className={`module-toggle ${active[index] ? "on" : ""}`} onClick={() => setActive((current) => current.map((value, itemIndex) => itemIndex === index ? !value : value))} title="Toggle availability"><span /></button><button className="table-more"><MoreHorizontal size={17} /></button></div>)}</div>{!items.length && <div className="admin-empty"><div><Check size={21} /></div><strong>Nothing here yet</strong><span>Add your first item to get started.</span></div>}</section></>; }
