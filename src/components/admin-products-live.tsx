"use client";
import Image from "next/image";
import Link from "next/link";
import { MoreHorizontal, Plus, Search } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { formatTnd } from "@/lib/admin-data";
type Product = { id: string; name: string; sku: string; price: number; stock: number; status: string; image_url: string | null; category?: { name?: string } | { name?: string }[] | null };
export function AdminProductsLive() { const [products, setProducts] = useState<Product[]>([]); const [query, setQuery] = useState(""); const [error, setError] = useState(""); useEffect(() => { fetch("/api/admin/products").then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error); setProducts(body.products); }).catch((caught) => setError(caught instanceof Error ? caught.message : "Unable to load products.")); }, []); const filtered = useMemo(() => products.filter((product) => `${product.name} ${product.sku}`.toLowerCase().includes(query.toLowerCase())), [products, query]); return <><div className="admin-page-heading"><div><p className="admin-eyebrow">Catalog</p><h1>Products</h1><p>{products.length} products in your database.</p></div><Link className="admin-dark-button" href="/admin/products/new"><Plus size={15} /> Add product</Link></div><section className="admin-panel"><div className="panel-heading"><h2>Product catalog</h2><label className="table-search"><Search size={15} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search products..." /></label></div>{error && <p className="admin-form-error">{error}</p>}<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Product</th><th>Category</th><th>SKU</th><th>Price</th><th>Stock</th><th>Status</th><th /></tr></thead><tbody>{filtered.map((product) => { const category = Array.isArray(product.category) ? product.category[0]?.name : product.category?.name; return <tr key={product.id}><td><span className="product-cell">{product.image_url ? <Image src={product.image_url} alt="" width={38} height={38} /> : <b className="module-art">N</b>}<span><strong>{product.name}</strong><small>{product.sku}</small></span></span></td><td>{category ?? "—"}</td><td>{product.sku}</td><td><strong>{formatTnd(Number(product.price))}</strong></td><td>{product.stock}</td><td><span className="stock-status good"><i />{product.status}</span></td><td><button className="table-more"><MoreHorizontal size={17} /></button></td></tr>; })}</tbody></table>{!error && !filtered.length && <div className="admin-empty"><strong>No products found</strong><span>Create your first product to populate the catalog.</span></div>}</div></section></>; }
